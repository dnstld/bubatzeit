import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  progressBarContainer: {
    flexDirection: 'row',
    gap: 4,
  },
  progressBar: {
    width: 75,
  },
  score: {
    flexDirection: 'row',
    gap: 4,
    alignItems: 'center',
  },
});
